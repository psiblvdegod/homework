#pragma once
#include <stdbool.h>

//checks the serviceability of the functions from the module sorting.c
bool treeSortTests();

//checks the serviceability of the functions from the module tree.c
bool treeTests();